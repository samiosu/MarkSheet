# マークシート入力・採点アプリ 要件案

この文書は、Codexに実装を依頼するための要件案です。「確定要件」は依頼内容に基づきます。それ以外は、初版を具体化するための推奨仕様です。追加の指示がある場合は、その指示を優先してください。

2026-09-08追記：解答の公開CSV／JSONは`label`と`answer`のみとし、正答の出力には`points`を含める形式に更新しました。正答の読込は配点なしの従来形式と配点付き形式の両方に対応します。ブラウザー内の保存は、構成・配点を含む従来の内部形式を維持します。

## 1. 目的と確定要件

GitHub Pagesで公開できる、マークシート形式の解答入力・ファイル出力・採点アプリを作成する。

- ブラウザー上で、各問の選択肢をマークして解答を入力できる。
- 入力した解答をCSVまたはJSONで出力できる。
- 画面への入力およびCSV／JSONのインポートを利用して採点できる。
- 開発者が用意する数字・カタカナの選択肢テンプレートを利用できる。
- ユーザーが任意の選択肢を作成して利用できる。
- 新規作成・新規ファイル読込・共通の選択肢設定で「自由に入力」を選べる。文字・数字・記号を直接指定し、追加・削除・並べ替えで個数を変更できる。4択固定にせず、2択・3択・5択以上を利用できる。
- GitHub Pagesへの配置に対応する。
- デザインの実装方針として、[emilkowalski/skills](https://github.com/emilkowalski/skills) の主スキル `emil-design-eng` を採用する。

用語は、ユーザーが選んだ内容を「解答」、採点の基準を「正答」、選択肢の並びを「選択肢テンプレート」、問題番号と選択肢を持つ一式を「シート」とする。

## 2. 初版の推奨範囲

以下は未指定事項に対する提案であり、依頼者が確定した条件ではない。

| 項目 | 初版で採用する推奨仕様 |
| --- | --- |
| ファイル形式 | CSVとJSONの両方を入出力できる |
| 選択方式 | 1問につき1つを選択する単一選択 |
| 採点対象 | 1回につき1人分の解答と1つの正答セット |
| 問題の内容 | 問題文は別途参照し、このアプリは問題番号と解答を管理する |
| 配点 | 標準は1問1点。正答編集時に問題別の正の整数へ変更可能 |
| 途中保存 | 現在の作業と自作テンプレートをブラウザー内に自動保存する |
| 利用環境 | 日本語UI。PC・スマートフォンに対応する |
| 構成 | 入力、ファイル変換、採点をブラウザー内で処理する |

初版の対象外は、複数選択・部分点、複数人の一括採点、問題文の編集、紙のマークシートの画像認識、ログイン、サーバーへの答案収集、端末間同期とする。必要になった時点で別途追加する。

## 3. 利用の流れ

### 3.1 解答を入力して出力する

1. シート名、問題数、共通の選択肢テンプレートを指定する。
2. 必要に応じて、一部の問題の番号・選択肢を変更する。
3. マークシート上で解答する。
4. 解答をCSVまたはJSONでダウンロードする。未回答を残した出力も可能とする。

### 3.2 解答を採点する

1. 解答を画面で入力するか、解答ファイルを読み込む。
2. 同じシートに対する正答を画面で入力するか、正答ファイルを読み込む。
3. 採点ボタンを押す。
4. 得点、正答率、問題別の判定を確認する。

解答と正答は、どちらも「手入力」「CSV」「JSON」を選べる。解答がCSVで正答がJSON、解答が手入力で正答がCSVといった組み合わせも対応する。

正答ファイルを先に読み込んだ場合は、ファイルの問題番号と画面で選んだ選択肢テンプレートから空の解答シートを作成できる。空の解答シートは、全問未回答の解答ファイルとして配布・再利用できる。

## 4. 画面と機能

### 4.1 シート設定

- シート名と問題数を設定できる。問題数は1問以上とする。
- 初期状態では問題番号を1から連番で作成する。
- 表示する問題番号は文字列とし、「1-1」「問2」「3(2)」などに編集できる。
- 問題の追加・削除・並べ替えができる。
- 全問共通の選択肢を設定した後、問題ごとに選択肢を変更できる。
- 編集によって既存の解答・正答が無効になる場合は、影響する問題を表示してから変更する。
- 選択肢を削除しても、既存の解答を別の選択肢へ自動的に置き換えない。

### 4.2 選択肢テンプレート

- 標準テンプレートとして数字・カタカナを用意する。
- 暫定の初期値は数字「1、2、3、4」、カタカナ「ア、イ、ウ、エ」とする。正式な範囲・個数が指定された場合は置き換える。
- 標準テンプレートは設定データとして定義し、開発者が種類や内容を変更しやすくする。
- ユーザーはテンプレート名と選択肢一覧を入力し、保存・編集・複製・削除できる。
- 選択肢は行単位で追加・削除・並べ替えできる。1行の入力欄に1つの選択肢を入れるため、選択肢内のカンマを区切り文字として扱わない。
- 数字やカタカナ以外にも「A／B／C」「○／×」「正しい／誤り」などを設定できる。
- 選択肢は2つ以上とし、前後の空白を除去した後の空文字・重複を禁止する。
- 選択肢は文字列として扱い、「01」と「1」などを勝手に同一化しない。
- テンプレートをシートに適用する際は内容をコピーする。テンプレートを後から編集しても、既存シートの選択肢を変更しない。

### 4.3 解答入力

- 問題番号と選択肢を行単位で表示し、丸いマークとラベルをクリック・タップして選択できる。
- 1問につき選べる選択肢は1つ。別の選択肢を選ぶと切り替わる。
- 各問に「解答を消す」操作を用意し、未回答に戻せる。
- 回答済み数／全問題数を表示し、未回答の問題を識別できる。
- キーボード操作、フォーカス表示、スクリーンリーダーのラベルに対応する。
- 全解答のクリア時は、消去対象を確認できる。

### 4.4 正答入力

- 解答入力とは別のモードにし、「正答を編集中」であることを常に明示する。
- 同じ問題構成を使って、各問の正答と配点を入力する。
- 解答と正答を別々の状態として保持し、モード切替で相互に上書きしない。
- 正答を未設定にできる。未設定の問題は採点対象外とする。
- 正答セットをCSV／JSONとして保存し、再利用できる。

### 4.5 インポート・エクスポート

- 解答と正答に、それぞれファイル読込・CSV出力・JSON出力を用意する。
- 読み込んだファイル名、読込先の種別、問題数、未設定の件数、内容のプレビュー、置換先を表示する。
- 解答の公開ファイルは問題番号と選択値のみを含み、正答の出力には問題別の配点も含める。既存シートへの再読込では、選択肢・シート名・反対側の入力を保持し、対象側の選択値を全問更新する。配点付き正答を読み込んだ場合だけ配点も更新する。
- 外部で作成したファイルも、公開する指定形式に一致していれば読み込める。任意の列名のCSVを自動推測する機能は初版に含めない。
- 形式違い、重複・不足・余分な問題番号、選択肢に含まれない値などは、理由と該当行・項目を表示する。
- ファイル全体の検証が成功してから反映する。途中まで取り込んだ状態にしない。
- 入力済みデータを置き換える場合は、対象を明示して確認する。解答の読込で正答を上書きしない。
- 新しいシートとして開く場合は明示的に選び、シート名と選択肢テンプレートを確認してから作業を置き換える。各問の配点は1点で開始する。

## 5. 採点ルール

| 解答と正答の状態 | 表示する判定 | 得点 | 正答率の分母 |
| --- | --- | --- | --- |
| 解答と正答が一致 | 正解 | その問題の配点 | 含める |
| 解答と正答が不一致 | 不正解 | 0点 | 含める |
| 解答が未回答、正答は設定済み | 未回答 | 0点 | 含める |
| 正答が未設定 | 採点対象外 | 集計しない | 含めない |

- 得点は正解した問題の配点合計、満点は採点対象の問題の配点合計とする。
- 正答率は「正解数 ÷ 採点対象の問題数 × 100」とし、小数第1位まで表示する。
- 配点に差がある場合、正答率と得点率は別の値になる。初版では「得点／満点」と「正答率」を明確に区別して表示する。
- 正答が全問未設定の場合は「採点対象がありません」と表示し、正答率を「—」とする。
- ファイル読込時は、問題番号（label）の集合が現在のシートと完全に一致し、各answerが画面で設定された選択肢に含まれることを確認する。ファイルの行順の違いは許容する。
- 問題数が同じという理由だけで対応付けない。対応しないファイルは自動採点せず、原因を表示する。
- 解答・正答・配点が変更された場合は結果を未採点に戻し、古い結果を最新として表示しない。
- 結果には、得点／満点、正答率、正解数、不正解数、採点対象の未回答数、採点対象外の問題数を表示する。
- 問題別一覧には、問題番号、自分の解答、正答、判定、獲得点／配点を表示する。色に加えて文字や記号でも判定を示す。

## 6. ファイル形式

解答の公開CSVとJSONは、各問題のlabelとanswerを持つ。正答の出力はlabel・answer・pointsを持つ。正答の読込ではpointsなしの従来形式も受け付け、読込先が正答の場合だけpointsを検証・反映する。どちらへ読み込むかは画面で選ぶこと。

### 6.1 共通モデル

| 項目 | 意味 |
| --- | --- |
| label | 画面に表示する問題番号の文字列。空・重複不可 |
| answer | 選択した文字列、または未回答・正答未設定を表すnull |
| points | 正答ファイルだけに含める正の整数の配点。配点付き形式では全問題に指定する |

種別、バージョン、シート名、ID、選択肢は公開ファイルに含めない。配点は正答の出力時だけ含める。CSV／JSONは作業全体のバックアップではなく、問題番号・選択値・正答の配点の入出力に用いる。

ブラウザー内部では問題IDを追加時に発行し、並べ替えで振り直さず、削除したIDを再利用しない。構成、選択肢、配点は内部保存に含め、既存の保存データを復元できるようにする。

### 6.2 JSON例：解答

```json
[
  { "label": "01", "answer": "イ" },
  { "label": "02", "answer": null }
]
```

最上位は配列とし、全問題についてlabelとanswerの両方を指定する。正答ファイルではanswerに正答を指定し、出力時はpointsも加える。読込時はanswerの空文字も未設定とし、出力ではnullを用いる。JSON内で数値型へ変換しない。

正答ファイルの出力には次のようにpointsを追加する。pointsなしの正答JSONも読み込めるが、既存シートでは現在の配点を保持し、新規シートでは1点で開始する。pointsを含める場合は全問題に正の整数を指定する。

```json
[
  { "label": "01", "answer": "イ", "points": 2 },
  { "label": "02", "answer": null, "points": 1 }
]
```

### 6.3 CSV例：解答

CSVは1行1問とし、次のヘッダーを使用する。

```csv
label,answer
01,イ
02,
```

- ヘッダーは`label,answer`の順とし、全行を2列にする。
- `answer`の空欄はJSONのnullに対応する。未回答でもその問題の行は含める。
- 正答の出力は`label,answer,points`の3列とする。正答の読込では2列・3列の両方を受け付け、配点付きの場合は全行に正の整数を指定する。解答の読込にpoints列を含めてはならない。
- UTF-8で入出力し、CSV出力にはBOMを付ける。読込ではBOMの有無とCRLF／LFを許容する。
- カンマ、引用符、改行を含むセルを正しく処理する。単純なカンマ分割で解析しない。
- 問題番号・選択値は文字列として読み込み、先頭ゼロなどを保つ。

### 6.4 検証と互換性

- JSONはUTF-8とし、拡張子だけでなく内容の構造も検証する。
- label・answer以外の項目は解答では拒否する。正答ではpointsだけを許可し、pointsの欠落・混在・0以下・小数・文字列を拒否する。共通して必須項目の欠落、空の一覧、空・重複した番号、不正な型を拒否する。
- 既存シートへの読込ではlabelの集合を完全一致で照合し、answerがその問題の選択肢に含まれることを検証する。nullは未設定として許容する。
- 問題の行順の違いはlabelで解決し、既存シートの表示順は保持する。現在の問題にないlabelや、ファイルにない問題は拒否する。
- 新規読込ではファイル中のlabelと行順を維持し、選択したテンプレートにないanswerを拒否する。値を推測して選択肢へ追加しない。
- IDがないため、同じ番号・選択値を持つ別の試験を自動判別することはできない。利用者が読込先を確認する。
- 旧版の9列CSVとメタデータ付きJSONは公開読込の対象外とする。旧版のブラウザー保存形式は維持する。正答の2列形式は互換入力として維持する。
- 自アプリから出力した形式を、仕様とサンプルファイルとしてREADMEに掲載する。

## 7. 保存・操作性

- 現在のシート、解答、正答、自作テンプレートをブラウザー内に保存し、再読み込み後に復元できる。
- 保存できない場合は知らせ、入力とファイル出力を継続できるようにする。
- ブラウザー内の保存は端末間で同期されない。画面には保存状態を示し、ファイルとして持ち出せるようにする。
- 保存データを削除する操作を用意する。
- 横幅の狭い画面や長い選択肢でも、番号と選択肢の対応が崩れないようにする。
- 少なくとも200問・各10選択肢のシートで、入力とスクロールを妨げないことを確認する。
- 読み込んだラベルなどはテキストとして表示し、HTMLとして実行しない。

## 8. デザイン・技術構成・GitHub Pages対応案

### 8.1 採用するデザインスキル

- 採用元は [emilkowalski/skills](https://github.com/emilkowalski/skills)、主に使用するスキルは [`emil-design-eng`](https://github.com/emilkowalski/skills/blob/main/skills/emil-design-eng/SKILL.md) とする。
- UIの設計・実装・変更・レビュー時は、このスキルを読み、コンポーネント、操作への反応、アニメーション、アクセシビリティの判断に適用する。
- プロジェクト内の配置先は `.agents/skills/emil-design-eng/` とする。配置・利用の手順は11章を参照する。
- スキルはCodexが開発時に参照する指示書として管理する。配布するWebアプリの動作にスキルファイルの読込を要求しない。
- スキル内の実装例は、実際に使用するライブラリのバージョンやブラウザー対応を確認して適用する。

このアプリでは、スキルの考え方を次のように適用する。

- 余白、文字サイズ、位置合わせ、色、ボタンの状態表現を全画面で統一する。
- 解答入力と正答編集を見分けやすくし、選択中・未回答・フォーカス中の状態を明確にする。
- マークの選択・解除は即座に反映する。繰り返し行う解答入力やキーボード操作には、待ち時間の生じるアニメーションを付けない。
- アニメーションはダイアログの開閉など、状態変化を理解するために役立つ箇所に絞る。大量の問題行を順番に出現させる演出は採用しない。
- 動きを付ける場合は短時間にとどめ、変更するCSSプロパティを明示する。`transition: all` による一括指定は使用しない。
- `prefers-reduced-motion` に対応し、動きを減らす設定では位置移動などを抑える。必要な状態表示は維持する。
- タッチ操作でも選択肢を押しやすくし、ホバーだけに依存した情報表示や操作を設けない。
- UIコードのレビュー結果は、スキルの指定に合わせて `Before / After / Why` の表で報告する。

アニメーションの追加を目的にせず、解答の入力速度、読みやすさ、操作の分かりやすさを判断基準にする。

### 8.2 技術構成と公開

- 新規実装の推奨構成はReact + TypeScript + Vite。既存プロジェクトがある場合は、その構成に合わせる。
- UIと、採点・検証・CSV／JSON変換の処理を分け、採点や変換を独立してテストできるようにする。
- ファイルの読込・出力、採点、保存はクライアント側で行う。利用者の解答や正答をサーバーへ送信する機能は設けない。
- GitHub PagesはHTML・CSS・JavaScriptを公開する静的ホスティングであり、この構成で動作させる。[GitHub公式ドキュメント](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)
- Viteの `base` を配置先に合わせる。プロジェクトサイトでは `/<repository>/`、ユーザーサイトのルートでは `/` とする。[Vite公式デプロイガイド](https://vite.dev/guide/static-deploy#github-pages)
- 初版の画面切替は同一ページ内のタブなどで実装する。URLルーティングを導入する場合は、GitHub Pagesで再読み込みしても404にならない構成にする。
- ビルドしてGitHub Pagesへ配置するGitHub Actions設定と、READMEの設定手順を用意する。アクションと依存関係のバージョンは実装時に確認する。

## 9. 完了条件

1. 数字・カタカナ・自作の各テンプレートでシートを作成できる。
2. 同じシート内で問題ごとに異なる選択肢を利用できる。
3. 選択、選択変更、未回答への復帰ができる。
4. 解答をlabelとanswerだけ、正答をlabel・answer・pointsのCSV／JSONで出力し、正答はpointsなしの旧形式も同じ番号と選択肢のシートへ再読込できる。配点付き正答の再読込では配点も復元し、旧形式では既存の配点を保持する。
5. 解答と正答について、手入力／CSV／JSONを組み合わせて採点できる。
6. 正解、不正解、未回答、採点対象外の判定と得点・正答率が仕様どおりになる。
7. 問題の行順が変わったファイルも、labelによって正しく対応付けて採点できる。
8. 番号が一致しないシート、不正な選択値、欠けた問題、破損ファイルは読み込まない。置換対象を確認し、失敗・キャンセル時は作業を保持する。
9. カンマ・引用符・日本語・先頭ゼロを含む値が、出力と再読込で保たれる。
10. ページ再読み込み後に作業を復元できる。
11. PCとスマートフォンで入力でき、キーボードでも選択・解除できる。
12. 本番ビルドが成功し、GitHub Pagesのリポジトリ配下のURLに相当する配置でアセットと画面が動作する。
13. `emil-design-eng` のスキルファイルと、UI実装時に使用する指示がプロジェクトに含まれている。
14. スキルに沿ったUIレビューを行い、連続入力、キーボード操作、動きを減らす設定でも操作を妨げないことを確認する。

採点の検証例：配点を順に2・1・3・1点、正答を「ア・イ・ウ・未設定」、解答を「ア・ウ・未回答・ア」とする。期待結果は2／6点、正答率33.3%、正解1問、不正解1問、未回答1問、採点対象外1問。

最低限の自動テストは、採点の境界条件、CSV／JSONの往復、ファイルの不一致検出を対象にする。加えて、作成→入力→出力→再読込→採点の代表的な操作を通して確認する。

## 10. Codexへの実装依頼

この要件に基づいてアプリを実装してください。初版の推奨仕様は、追加の指示がない限り採用してください。既存リポジトリの方針を確認し、UI、入出力、採点、保存、GitHub Pages用ビルド設定を完成させてください。

UIの実装前に `.agents/skills/emil-design-eng/SKILL.md` を読み、`$emil-design-eng` を適用してください。未導入の場合は11章に従って配置してください。スキルが指定するデザイン方針を、このアプリの用途に合わせて適用し、8.1節の基準を満たしてください。

成果物として、アプリのソースコード、主要ロジックのテスト、CSV／JSONのサンプル、起動・ビルド・GitHub Pages設定手順を含むREADMEを用意してください。実装した範囲と検証結果を報告し、実際の公開確認をしていない場合は、その点を明記してください。

## 11. デザインスキルの配置・利用手順

### 11.1 推奨：アプリのリポジトリ内に導入する

アプリのリポジトリ直下で、次のコマンドを実行する。Node.js（npm／npx）とGitが利用できる環境を前提とする。

```bash
npx skills@latest add emilkowalski/skills --skill emil-design-eng --agent codex --copy
```

- `--skill emil-design-eng`：このアプリで採用する主スキルを選ぶ。
- `--agent codex`：Codex用に配置する。
- `--copy`：ファイルをコピーして配置する。
- `--global` は指定せず、プロジェクト単位で導入する。配置範囲を聞かれた場合はProjectを選ぶ。

配布元のREADMEは `npx skills@latest add emilkowalski/skills` による導入を案内している。上記は対象スキルとエージェントを絞ったコマンドである。[配布元の導入案内](https://github.com/emilkowalski/skills#install)、[skills CLIのオプション](https://github.com/vercel-labs/skills#options)

Codexは、リポジトリ内の `.agents/skills` にあるスキルを検出する。[OpenAI公式ドキュメント](https://learn.chatgpt.com/docs/build-skills#where-codex-loads-local-skills)

| アプリのリポジトリ直下からのパス | 役割 |
| --- | --- |
| `mark-sheet-requirements.md` | この要件書 |
| `AGENTS.md` | UI作業時にスキルを使う指示 |
| `.agents/skills/emil-design-eng/SKILL.md` | デザインスキル本体 |
| `.agents/skills/emil-design-eng/` 内の関連ファイル | 配布元に参考資料などがある場合は一緒に保持する |
| `skills-lock.json` | 導入ツールが生成した場合、スキルの取得元などの管理に使う |
| `src/` | アプリの実装 |

スキルのフォルダー、`AGENTS.md`、要件書、生成されたロックファイルをアプリのGit管理に含める。これにより、別のPCやリポジトリをチェックアウトするCodex環境でも同じ指示を参照できる。更新時は差分を確認してから反映する。

### 11.2 手動で配置する場合

1. [配布元リポジトリ](https://github.com/emilkowalski/skills) をZIPで取得するか、作業用の場所にcloneする。
2. 配布元の `skills/emil-design-eng/` フォルダーを、アプリ側の `.agents/skills/emil-design-eng/` にコピーする。参考資料が追加されている場合も、フォルダーの構成を保つ。
3. 配布元ルートの [`LICENSE`](https://github.com/emilkowalski/skills/blob/main/LICENSE) を、コピー先のスキルフォルダーに同梱する。CLIで導入した場合も、同梱されていなければ追加する。
4. コピーした内容をアプリのGit管理に含める。元リポジトリの `.git` はコピー対象に含めない。

配置後は `.agents/skills/emil-design-eng/SKILL.md` が存在することを確認する。単に配布元へのURLを要件書へ書くだけで、自動インストールされた状態にはならない。

### 11.3 Codexに確実に適用させる

アプリのリポジトリ直下の `AGENTS.md` に、次の指示を追加する。既存の `AGENTS.md` がある場合は、その内容を保って追記する。

```markdown
## UIの設計・実装

- UIを設計・実装・変更・レビューする際は、`.agents/skills/emil-design-eng/SKILL.md` を読み、`emil-design-eng` を適用する。
- 機能と採点仕様は `mark-sheet-requirements.md` に従う。
- マークの連続入力とキーボード操作は即座に反映し、演出による待ち時間を設けない。
- `prefers-reduced-motion` とタッチ操作に対応する。
```

`AGENTS.md` は、Codexにプロジェクトの作業方針を伝えるためのファイルである。[OpenAI公式ドキュメント](https://learn.chatgpt.com/docs/agent-configuration/agents-md)

CLI経由の導入状況は、プロジェクト内で次のコマンドにより確認できる。

```bash
npx skills@latest ls --agent codex
```

Codex CLIやIDE拡張では、`/skills` または `$` の候補から `emil-design-eng` を確認する。新しいスキルが表示されない場合はCodexを再起動する。実装依頼の冒頭に `$emil-design-eng` を指定すると、明示的に使用させられる。[OpenAI公式ドキュメント](https://learn.chatgpt.com/docs/build-skills#how-chatgpt-and-codex-use-skills)

依頼例：

```text
$emil-design-eng を使い、mark-sheet-requirements.md に従ってアプリを実装してください。
```
